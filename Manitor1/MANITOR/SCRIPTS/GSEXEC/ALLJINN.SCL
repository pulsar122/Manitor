main ()
{
  if ( address("manitor") )
  {
    deactivate();     # deaktivieren (falls er lief)
    preset();         # Grundeinstellungen
    include_prgs();
    show_prg("jinnee");
    activate();       # Manitor aktivieren
  }
}
